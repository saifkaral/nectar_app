import 'package:flutter/material.dart';

class RowOfCards extends StatelessWidget {
  const RowOfCards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
